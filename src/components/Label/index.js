import React from 'react';
import styled from 'styled-components';
const LabelWrapper = styled.div`
  
`;
const Label = props => {
    return (
        <div>
          {props.children}
        </div>
    )
}
